[tool.pytest.ini_options]
# Pytest configuration
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

# Disable doctest collection
addopts = "--ignore=pyvarchart.py"

# Only run tests from tests/ directory, not from pyvarchart.py

